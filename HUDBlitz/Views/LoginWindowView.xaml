<Window x:Class="HUDBlitz.Views.LoginWindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HUDBlitz.Views"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="LoginWindowView" Height="250" Width="400">

    <Grid>
        <Label x:Name="DebugLabel"/>
        <!-- Ввод токина -->
        <StackPanel x:Name="TokenPanel" Visibility="Visible" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Введите токен"/>
            <PasswordBox x:Name="UserKey" Margin="0 0 0 0"/>
            <Button x:Name="btnEnterToken" Click="btnEnterToken_Click" Margin="0 5 0 5" Content="Далее"/>
        </StackPanel>

        <!-- Ввод логина и пароля -->
        <StackPanel x:Name="AuthPanel" Visibility="Hidden" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Пароль"/>
            <PasswordBox x:Name="PassworBoxdUser" Margin="0 0 0 0"/>
            <Button  x:Name="btnLogin" Content="Включить HUD" Margin="0 5 0 5" Click="btnLogin_Click"/>
        </StackPanel>
    </Grid>
</Window>
